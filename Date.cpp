#include <iostream>

#include "Date.h"

Date::Date(int day, int month, int year) {
    this -> day = day;
    this -> month = month;
    this -> year = year;

}

void Date::DisplayDate(Date::FormatType dateFormat = Short) {
    if (dateFormat == Date::FormatType Short) {
        cout << 
        return;
    }
    
}